<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <application
        android:allowBackup="true"
        android:label="My Tracks App"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <!--
          T09 — Google Maps SDK for Android API key. Value is sourced from
          app/src/main/res/values/google_maps_api.xml, which is a placeholder
          ("YOUR_API_KEY_HERE") and MUST be replaced by a developer's own key from Google
          Cloud Console before the map will render real tiles. See
          docs/setup/google-maps-api-key.md.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_api_key" />

        <service
            android:name=".service.LocationForegroundService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="My Tracks App"
            android:theme="@android:style/Theme.Material.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
